# Close browser session

Closes the browser session and frees resources.

## Usage

``` r
comex_close()
```

## Value

Invisibly returns TRUE

## Examples

``` r
if (FALSE) { # \dontrun{
comex_close()
} # }
```
